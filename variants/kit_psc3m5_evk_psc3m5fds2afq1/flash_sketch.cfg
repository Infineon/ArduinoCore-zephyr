targets psc3.cm33

if {[catch {program "${filename0}" verify}] != 0} {
    program "${filename0}"
}
if {[catch {verify_image "${filename1}" ${offset} bin}] != 0} {
    flash write_image erase "${filename1}" ${offset} bin
}
reset run
shutdown